### Plans made on 2026-03-28

## Overview

This will take quite some time for full implementation, so we will go step by step, ensuring we don't take on too much at one time.

## Create helper functions.

* Function to draw top of table of width x, using TaipanThickFont characters.  Optional flags will remove left corner and right corner separately, so we can draw partial boxes and plain lines.
* Function to draw bottom of table of width x, using TaipanThickFont characters.  Optional flags will remove left corner and right corner separately, so we can draw partial boxes and plain lines.
* Function to draw bottom and top of two tables of width x, using TaipanThickFont characters
* Function to draw a table row of width x.  First and last character will be TaipanThickFont vertical lines.  Inside text will be standard Taipan font.

## Support text input

Add support for text input with cursor.  This will use TaipanStandardFont and the cursor will be a blinking long underscore (full character width)

## Add firm name screen

We will add a screen asking for your firm name.  Firm name will show in TaipanThickFont.  It will use the test input with cursor mentioned above.  We will refer to screenshot to design this.

## Add status panel

Add the main status panel that shows at all times, except during battle

## Add original Apple 2 style text and menus under the status panel.

## Add ship sprites

## Add original Apple 2 style battle screen, using ship sprites, and table helper functions to draw the partial box in the top right.

## Add sounds (see sounds folder)

Sounds have already been uploaded to Studio, and just need to be integrated at the appropriate times.

## Add graphical glitch layer

## Add text slowness, to simulate actual Apple 2 (make this configurable)
